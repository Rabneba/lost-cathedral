import fs from'node:fs/promises';import{GLTFLoader}from'three/addons/loaders/GLTFLoader.js';
export async function loadGLB(path){const b=await fs.readFile(path),n=b.readUInt32LE(12),j=JSON.parse(b.subarray(20,20+n));j.buffers[0].uri='data:application/octet-stream;base64,'+b.subarray(28+n).toString('base64');for(const m of j.meshes||[])for(const p of m.primitives)delete p.material;for(const k of['images','textures','samplers','materials'])delete j[k];globalThis.ProgressEvent??=class{constructor(t,i){Object.assign(this,i);}};return new GLTFLoader().parseAsync(JSON.stringify(j),'');}
