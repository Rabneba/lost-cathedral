import {readFile, writeFile, stat} from 'node:fs/promises';
import {relative} from 'node:path';

const brief=JSON.parse(await readFile('docs/boss-greatsword-briefs.json','utf8'));
const jobs=JSON.parse(await readFile('docs/boss-greatsword-jobs.json','utf8'));
const escape=text=>text.replaceAll('&','&amp;').replaceAll('"','&quot;').replaceAll('<','&lt;');
const cards=[];
for(const job of jobs){
  const result=JSON.parse(await readFile(`docs/boss-greatsword-result-${job.key}.json`,'utf8'));
  if(result.status!=='completed'||!result.saved?.length)throw new Error(`${job.key} is not downloaded yet`);
  const asset=result.saved[0];
  await stat(asset.path);
  Object.assign(job,{status:'completed',localPath:asset.path,sourceUrl:asset.url,bytes:asset.bytes});
  delete job.nextCommand;
  const concept=brief.concepts.find(item=>item.key===job.key);
  const src=relative('assets',asset.path);
  cards.push(`<article><div class="heading"><span class="number">0${job.number}</span><div><h2>${job.name}</h2><p>${escape(concept.description)}</p></div></div><a class="art" href="${src}" data-title="${job.number} / ${job.name}" aria-label="Enlarge ${job.name}"><img src="${src}" alt="${escape(concept.description)}"></a></article>`);
}
await writeFile('docs/boss-greatsword-jobs.json',JSON.stringify(jobs,null,2)+'\n');

const page=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Iron and ruin — two greatsword boss concepts</title><style>
*{box-sizing:border-box}html{color-scheme:dark}body{margin:0;background:#131516;color:#e9e4dc;font:15px/1.6 system-ui,sans-serif}main{max-width:1780px;margin:auto;padding:28px 30px 40px}.eyebrow{font-size:10px;text-transform:uppercase;letter-spacing:.19em;color:#b29a80;margin:0 0 7px}header{display:flex;align-items:end;justify-content:space-between;gap:28px;margin-bottom:24px}h1{font:400 clamp(32px,4vw,49px)/1.15 Georgia,serif;margin:0 0 10px;letter-spacing:-.03em}header p{margin:0;color:#a7a8a4;max-width:820px}.stage{white-space:nowrap;font-size:11px;text-transform:uppercase;letter-spacing:.12em;color:#b9a38a;padding:8px 13px;border:1px solid #554638}.grid{display:grid;grid-template-columns:1fr 1fr;gap:24px}article{border:1px solid #3d3b37;background:#1d2021;min-width:0}.heading{display:flex;align-items:center;gap:17px;padding:19px 21px;min-height:97px}.number{font:32px Georgia,serif;color:#aa8a69;padding-right:17px;border-right:1px solid #544432}h2{font:400 25px/1.15 Georgia,serif;margin:0 0 6px}.heading p{font-size:12px;color:#aaa9a3;margin:0}.art{display:block;background:#171b1d;cursor:zoom-in}.art img{display:block;width:100%;height:min(67vh,820px);object-fit:contain}.note{display:flex;justify-content:space-between;gap:20px;align-items:center;margin-top:22px;color:#a8a69f;font-size:13px}.note p{margin:0}a{color:#bfc9c9;text-underline-offset:5px}details{margin-top:26px;padding-top:18px;border-top:1px solid #3d3b37}summary{cursor:pointer;color:#a9b2b3;font-size:13px}.source{margin:17px 0 0;display:block;max-width:650px;width:100%}footer{font-size:11px;color:#7d827f;margin-top:19px}dialog{padding:0;background:#090c0d;border:1px solid #5e615b;color:#e8e3da;width:96vw;max-width:1700px;height:95vh;max-height:95vh}dialog::backdrop{background:#000e}.bar{display:flex;align-items:center;gap:9px;padding:10px 16px;height:61px;background:#232727}.bar strong{flex:1;font:21px Georgia,serif}.bar button{background:#36332e;color:#e6d8c7;border:1px solid #796956;padding:8px 13px;cursor:pointer}.full{display:block;width:100%;height:calc(100% - 61px);object-fit:contain}a:focus-visible,button:focus-visible,summary:focus-visible{outline:2px solid #c4ac8b;outline-offset:4px}@media(max-width:850px){main{padding:23px 16px}header{display:block}.stage{display:inline-block;margin-top:18px}.grid{grid-template-columns:1fr;gap:22px}.art img{height:auto}.note{display:block}.note a{display:inline-block;margin-top:10px}.heading{padding:16px;gap:12px}h2{font-size:23px}.bar strong{font-size:16px}}
</style></head><body><main><header><div><p class="eyebrow">Armored Death / Two variations</p><h1>Iron and ruin.</h1><p>The heavy hood and worn armor you liked, with a few distinct details and two enormous rusted swords. Click either image to inspect it.</p></div><span class="stage">Concept review</span></header><section class="grid" aria-label="Two greatsword boss concepts">${cards.join('\n')}</section><div class="note"><p>Both swords are shown separately. The body keeps ordinary humanoid anatomy.</p><a href="boss-reaper-v2-review.html">Previous reference study ↗</a></div><details><summary>The starting image you liked</summary><img class="source" src="${relative('assets',brief.reference)}" alt="The previous hooded armored reference study, used as the direct image input for both variations."></details><footer>Generated with Genex using the previous reference study as the image input, at high quality. All images saved locally. Final character and sword selection remain yours.</footer></main><dialog aria-labelledby="full-title"><div class="bar"><strong id="full-title"></strong><button id="previous" aria-label="Previous concept">←</button><button id="next" aria-label="Next concept">→</button><button id="close">Close</button></div><img class="full" alt=""></dialog><script>
const links=[...document.querySelectorAll('.art')],dialog=document.querySelector('dialog'),picture=dialog.querySelector('img');let current=0;function show(index){current=(index+links.length)%links.length;const link=links[current];picture.src=link.href;picture.alt=link.querySelector('img').alt;document.querySelector('#full-title').textContent=link.dataset.title;}links.forEach((link,index)=>link.addEventListener('click',event=>{if(event.ctrlKey||event.metaKey||event.shiftKey||event.altKey)return;event.preventDefault();show(index);dialog.showModal();}));document.querySelector('#previous').addEventListener('click',()=>show(current-1));document.querySelector('#next').addEventListener('click',()=>show(current+1));document.querySelector('#close').addEventListener('click',()=>dialog.close());dialog.addEventListener('keydown',event=>{if(event.key==='ArrowLeft'){event.preventDefault();show(current-1);}if(event.key==='ArrowRight'){event.preventDefault();show(current+1);}});
</script></body></html>`;
await writeFile('assets/boss-greatsword-review.html',page);
console.log(JSON.stringify({review:'assets/boss-greatsword-review.html',images:jobs.map(job=>job.localPath)}));
